#include <stdio.h>

/**
 * main - entry point
 * Prints all singles digit numbers of base 10 staring from 0 - 9
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	prinft("\n");

	return (0);
}
